import React from "react";
import css from "./hrLine.module.css";

const HrLine = () => {
  return (
    <fieldset className={css.fieldStyle}>
      <legend>Or</legend>
    </fieldset>
  );
};

export default HrLine;
